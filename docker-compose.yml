services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./api/second_brain:/app/api/second_brain
      - ${LLM_MODEL_HOST_PATH}:${LLM_MODEL_PATH}:ro
    restart: unless-stopped
